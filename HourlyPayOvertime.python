#!/usr/bin/python

rate = input("What is your pay rate? $");
hours = input("How many hours did you work? ");

if (hours < 40):
    pay = rate * hours;
else:
    pay = (rate * 40) + ((hours-40) * (rate * 1.5));

print "Your paycheck will be $" , pay;
